import { DashboardResponsePayload } from './dashboard-response-payload';

describe('DashboardResponsePayload', () => {
  it('should create an instance', () => {
    expect(new DashboardResponsePayload()).toBeTruthy();
  });
});
